org: orgDemo # (optional) serverless dashboard org. default is the first org you created during signup.
app: appDemo # (optional) serverless dashboard app. default is the same as the name property.
stage: dev # (optional) serverless dashboard stage. default is dev.
component: flask@dev # (required) name of the component. In that case, it's flash.
name: flashDemo # (required) name of your flash component instance.

inputs:
  src:
    hook: 'pip3 install -r requirements.txt -t ./'
    dist: ./ # (optional) path to the source folder. default is a hello world app.
    exclude:
      - .env
  region: ap-guangzhou
  runtime: Python3.6
  apigatewayConf:
    protocols:
      - http
      - https
    environment: release
